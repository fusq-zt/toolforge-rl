# Demo

`toolforge_demo.py` runs one local model episode with real tool execution and
prints the full trace. It requires the base model and, optionally, a trained
adapter at paths supplied on the command line.

`example_trace.json` is the retained final-adapter example from Gate 7. It is a
small output artifact for inspection; the model weights and adapter are not
redistributed in this repository.
